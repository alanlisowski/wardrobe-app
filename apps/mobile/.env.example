# Expo public env vars (prefix required — these are inlined into the JS bundle).
# Copy to .env and fill in your values.

# Point at your API server. For Expo Go on a physical phone, use your
# machine's LAN IP instead of localhost (e.g. http://192.168.1.42:3001).
EXPO_PUBLIC_API_URL=http://localhost:3001
